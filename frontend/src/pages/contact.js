import * as React from "react";
import { Layout } from "../components/ui/layout";

const ContactPage = () => {
  return (
    <Layout>
      <p>Contact</p>
    </Layout>
  );
};

export default ContactPage;
