import * as React from "react";
import { Layout } from "../components/ui/layout";

const NotFoundPage = () => {
  return (
    <Layout>
      <h1>404: Not found</h1>
      <p>You just hot a route that doesn&#39;t exist.... the sadness.</p>
    </Layout>
  );
};

export default NotFoundPage;
